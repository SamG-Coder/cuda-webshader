import {chromium} from 'playwright';import {writeFile} from 'node:fs/promises';
const browser=await chromium.launch({headless:true,executablePath:process.env.CW_CHROMIUM||'C:/Program Files (x86)/Microsoft/Edge/Application/msedge.exe'});
try{const page=await browser.newPage();await page.goto('http://localhost:5173/');const report=await page.evaluate(async()=>{
 const {GpuRuntime}=await import('/src/runtime/runtime.js'),{mpiSqrtFixture}=await import('/showcases/nvidia/mpi-sqrt-fixtures.js'),runtime=await GpuRuntime.create(),results=[];
 try{const adapter=runtime.describe();if(!/nvidia/i.test(JSON.stringify(adapter)))throw Error('Real NVIDIA adapter required');const source=await(await fetch('/showcases/nvidia/kernels/34.cu')).text();for(const [groups,threads]of [[1,1],[1,32],[2,128],[3,256]]){const f=mpiSqrtFixture(groups,threads,16),resources=Object.fromEntries(Object.entries(f.buffers).map(([name,data])=>[name,runtime.createBuffer(data)]));try{const kernel=await runtime.kernel(source,{workgroupSize:[threads,1,1]});runtime.batch().dispatch(kernel.bind(resources,{}),f.groups).submit();const output=await runtime.read(resources.output),guardsPreserved=output.slice(groups*threads).every(v=>v===-12345);let maxRelativeError=0;for(let i=0;i<groups*threads;i++)if(f.expected[i])maxRelativeError=Math.max(maxRelativeError,Math.abs(output[i]-f.expected[i])/f.expected[i]);results.push({groups,threads,n:groups*threads,maxRelativeError,guardsPreserved,pass:guardsPreserved&&output.every((v,i)=>Number.isFinite(v)&&Math.abs(v-f.expected[i])<=f.absoluteTolerance+f.relativeTolerance*Math.abs(f.expected[i]))});}finally{await runtime.idle();for(const r of Object.values(resources))runtime.destroyBuffer(r);}}
 return {date:new Date().toISOString(),adapter,softwareAdapterRequested:false,sourceCompiled:true,absoluteTolerance:0,relativeTolerance:2e-7,scope:'Isolated square-root kernel; no MPI communication',results,passed:results.every(r=>r.pass)};
 }finally{runtime.dispose();}
 });await writeFile('reports/nvidia-mpi-sqrt.json',JSON.stringify(report,null,2));console.log(JSON.stringify(report,null,2));if(!report.passed)process.exitCode=1;
}finally{await browser.close();}
