import {chromium} from 'playwright';import {readFile,writeFile} from 'node:fs/promises';import {createStaticServer} from './serve.mjs';import {decodePPM} from '../src/sandbox/ppm.js';import {postReference} from '../tests/postprocess-gl-cases.js';
const server=createStaticServer(new URL('../dist/',import.meta.url).pathname.replace(/^\/(\w:)/,'$1'));await new Promise(r=>server.listen(0,'127.0.0.1',r));const browser=await chromium.launch({headless:true,executablePath:'C:/Program Files (x86)/Microsoft/Edge/Application/msedge.exe'});
try{const page=await browser.newPage({viewport:{width:1500,height:1000}});await page.goto('http://127.0.0.1:'+server.address().port+'/sandbox.html?example=postprocess');const wait=async n=>{await page.waitForFunction(n=>window.sandbox?.completedRuns===n||window.sandbox?.lastError,n,{timeout:60000});if(await page.evaluate(()=>window.sandbox.lastError))throw Error(await page.evaluate(()=>window.sandbox.lastError));};await wait(1);const device=await page.evaluate(()=>window.sandbox.runtime.describe());if(device.vendor!=='nvidia')throw Error('Hardware required');const native=await readFile('reports/postprocess-gl-native-4.bin');const compare=async expected=>{const pixels=await page.locator('#preview canvas').evaluate(c=>Array.from(c.getContext('2d').getImageData(0,0,c.width,c.height).data));if(pixels.length!==512*512*4)throw Error('Preview size');let maxError=0;for(let i=0;i<512*512;i++){for(let c=0;c<3;c++)maxError=Math.max(maxError,Math.abs(pixels[i*4+c]-expected[i*4+c]));if(pixels[i*4+3]!==255)throw Error('Preview alpha');}if(maxError>1)throw Error('Preview mismatch '+maxError);return maxError;};const nativeError=await compare(native);await page.locator('#tab-compare').click();if(!await page.locator('#wgsl-column').isVisible())throw Error('WGSL missing');await page.screenshot({path:'reports/postprocess-gl-sandbox.png'});await page.locator('#settings summary').click();const config=JSON.parse(await page.locator('#config').inputValue());config.scalars.highlight=1;await page.locator('#config').fill(JSON.stringify(config));await page.locator('#run').click();await wait(2);const input=decodePPM(await readFile('showcases/postprocess-gl/teapot.ppm')),expected=postReference(input.data,{w:512,h:512,r:4,threshold:.8,highlight:1}),editedError=await compare(new Uint8Array(expected.buffer));await page.setViewportSize({width:390,height:844});await page.waitForFunction(()=>document.documentElement.scrollWidth<=innerWidth);await writeFile('reports/postprocess-gl-sandbox-check.json',JSON.stringify({passed:true,staticBuild:true,softwareAdapterRequested:false,device,pixelsCompared:262144,nativeError,editedError,shaderComparison:true,mobileLayout:true},null,2));console.log('PASS postProcessGL sandbox: native pixels, edited highlight, WGSL comparison and mobile layout');}finally{await browser.close();server.closeAllConnections();await new Promise(r=>server.close(r));}
