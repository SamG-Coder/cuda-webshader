import {readFile,writeFile} from 'node:fs/promises';
const root=new URL('../reports/',import.meta.url),web=JSON.parse(await readFile(new URL('comparison-webgpu.json',root),'utf8')),cuda=(await readFile(new URL('comparison-cuda.jsonl',root),'utf8')).trim().split(/\r?\n/).map(JSON.parse);
if(web.error||web.results?.length!==20||cuda.length!==20)throw new Error('Expected 20 complete results from each API');
const rows=web.results.map(w=>{const c=cuda.find(c=>c.key===w.key);if(!c?.verified||w.iterations!==c.iterations||Object.values(w.checks).some(x=>!x.pass))throw new Error(`Invalid comparison ${w.key}`);return {key:w.key,id:w.id,scalars:w.scalars,webgpu:w,cuda:c,ratio:w.gpuMedianMs/c.gpuMedianMs};});
const fmt=n=>(n*1000).toFixed(2),range=values=>`${fmt(Math.min(...values))}–${fmt(Math.max(...values))}`;
let md=`# Measured CUDA versus WebGPU — RTX 5080\n\nMeasured ${web.date}. Windows WDDM, NVIDIA driver 616.64, CUDA Toolkit 13.3, NVCC -O3 -std=c++17 -arch=native, ${web.userAgent}. WebGPU reports ${web.device.vendor} / ${web.device.architecture}; nvidia-smi and native CUDA identify the RTX 5080, the only NVIDIA GPU in this machine.\n\nAll 20 workloads passed independent numerical references on both APIs. The same ten original CUDA kernel files run through NVCC and through this project's CUDA-to-WGSL compiler. Float32 tolerance is 0.001 absolute + 0.001 relative; integer histograms must match exactly. The separate 79-case WebGPU suite checks edge cases, and five additional native CUDA checks cover tails and hierarchical reduction.\n\n## Timing contract\n\nValues below are median microseconds per complete operation; lower is faster. Nine samples follow five warm-up operations. Each sample batches 2,048 operations (512 for matrix multiplication). WebGPU timestamps surround the compute pass; CUDA events surround a pre-instantiated, uploaded graph containing the matching operations. CUDA graph creation and WebGPU shader/pipeline compilation are excluded. Both APIs reuse GPU-resident buffers, reset inputs before each sample outside timing, and execute the same sequence within each batch. SAXPY accumulates y and particles advance with fixed dt/time during the batch. Reduction includes every level; histogram includes a 256-thread clear kernel before each histogram. Particle measurements cover compute only.\n\nThe GPU ratio is WebGPU / CUDA graph time: above 1 means CUDA was faster. These are batched execution costs, not isolated instruction latency. CUDA ordinary launches are measured separately and include gaps caused by CPU submission. WebGPU wall time includes JS command encoding, uniform upload, submission and completion waiting. CUDA graph wall time excludes graph construction; therefore those wall columns describe different host workflows, not equal CPU overhead. Allocation, compilation, initial uploads and result downloads are excluded from all timing columns.\n\nRepeated buffers can reside in cache; logical bandwidth is not DRAM bandwidth. Clock changes, WDDM scheduling and browser timestamp quantization remain sources of noise. Raw sample ranges are available in the HTML and JSON. Near-equal results are not evidence of a universal winner. No cuBLAS, tensor cores, transfer-heavy workload or rendering FPS comparison was performed.\n\n`;
for(const scale of ['small','large']){
 md+=`## ${scale==='small'?'Small':'Large'} workloads\n\n${scale==='small'?'262,144 vector elements; 128×128 matrices; 512×512 transpose; 65,536 particles.':'4,194,304 vector elements; 512×512 matrices; 2048×2048 transpose; 262,144 particles.'}\n\n| Kernel | WebGPU GPU µs | CUDA graph GPU µs | WebGPU / CUDA | CUDA ordinary GPU µs | WebGPU wall µs | CUDA graph wall µs |\n|---|---:|---:|---:|---:|---:|---:|\n`;
 for(const r of rows.filter(r=>r.key.startsWith(scale)))md+=`| ${r.id} | ${fmt(r.webgpu.gpuMedianMs)} | ${fmt(r.cuda.gpuMedianMs)} | ${r.ratio.toFixed(2)}× | ${fmt(r.cuda.directGpuMedianMs)} | ${fmt(r.webgpu.wallMedianMs)} | ${fmt(r.cuda.wallMedianMs)} |\n`;
 md+='\n';
}
md+=`## Reproduce\n\nFrom the project directory in PowerShell:\n\n\`\`\`powershell\nnpm ci\nnpm test\n$env:CW_CHROMIUM='C:\\Program Files (x86)\\Microsoft\\Edge\\Application\\msedge.exe'\n$env:CW_BENCH='1'\nnpm run test:gpu\nnpm run bench:native:build\n.\\reports\\native-reference.exe\n.\\reports\\native-benchmark.exe > reports/comparison-cuda.jsonl 2> reports/native-checks.log\nnpm run bench:webgpu\nnpm run bench:report\n\`\`\`\n\nRun the APIs sequentially with the live particle app and other GPU-heavy applications closed. The build script accepts -VcVars for a different Visual Studio installation. Set CW_CHROMIUM to your preferred WebGPU Chromium browser. This report records this run, not prefilled estimates.\n\n## Implementation and sources\n\n- benchmarks/cases.js: shared deterministic data and independent CPU formulas.\n- benchmarks/native-support.cuh: actual CUDA launches, verification, CUDA events, graph replay and ordinary launch timing.\n- reports/comparison-inputs/native-generated.cu: concrete runnable launches for all twenty cases, generated from the shared manifest.\n- benchmarks/webgpu.js: matched WGSL execution, readback checks and timestamps.\n- comparison-webgpu.json and comparison-cuda.jsonl: every raw timing sample.\n- [NVIDIA CUDA Graphs](https://docs.nvidia.com/cuda/cuda-programming-guide/04-special-topics/cuda-graphs.html): predefining a workflow reduces repeated CPU launch costs.\n- [NVIDIA CUDA event timing](https://developer.nvidia.com/blog/how-implement-performance-metrics-cuda-cc/): event-based GPU timing and synchronization boundaries.\n- [WebGPU specification](https://gpuweb.github.io/gpuweb/): timestamp queries and API execution semantics.\n`;
await writeFile(new URL('performance-comparison.md',root),md);
await writeFile(new URL('comparison-summary.json',root),JSON.stringify({date:web.date,rows},null,2));
const cards=rows.map(r=>`<tr data-scale="${r.key.split('-')[0]}"><td>${r.id}<small>${r.key.split('-')[0]}</small></td><td>${fmt(r.webgpu.gpuMedianMs)}<small>${range(r.webgpu.timings.map(t=>t.gpuMs))}</small></td><td>${fmt(r.cuda.gpuMedianMs)}<small>${range(r.cuda.timings.map(t=>t.gpuMs))}</small></td><td><b>${r.ratio.toFixed(2)}×</b><div class="bar" style="width:${Math.min(100,r.ratio*25)}%"></div></td><td>${fmt(r.cuda.directGpuMedianMs)}</td><td>${fmt(r.webgpu.wallMedianMs)}</td></tr>`).join('');
await writeFile(new URL('performance-comparison.html',root),`<!doctype html><html lang="en"><meta charset="utf-8"><meta name="viewport" content="width=device-width"><title>RTX 5080 · CUDA / WebGPU</title><style>body{margin:0;background:#10191d;color:#e4ece8;font:16px system-ui}main{max-width:1180px;margin:auto;padding:50px 28px}h1{font-size:44px;letter-spacing:-2px;margin:10px 0}p{max-width:1000px;line-height:1.65;color:#b4c4c3}a{color:#b8ef7f}small{display:block;color:#91a5a8;font-size:12px;margin-top:5px}table{width:100%;border-collapse:collapse;margin-top:24px}th,td{text-align:left;padding:15px 12px;border-bottom:1px solid #314146}th{color:#b8ef7f;font-size:13px}.bar{height:4px;background:#b8ef7f;margin-top:7px}select{padding:12px;background:#26363b;color:white;border:1px solid #506067;border-radius:6px}.scroll{overflow:auto}.badge{color:#b8ef7f;text-transform:uppercase;letter-spacing:2px;font-size:13px}</style><main><div class="badge">Measured on your GPU · 20 / 20 verified on both APIs</div><h1>CUDA ↔ WebGPU</h1><p>RTX 5080 · CUDA 13.3 · NVIDIA driver 616.64 · ${new Date(web.date).toISOString().slice(0,10)} UTC<br>Ten identical kernel sources, two sizes, real GPU execution. Median microseconds per operation. Small text shows the observed sample range.</p><p><b>WebGPU / CUDA above 1 means CUDA graph replay was faster.</b> CUDA graph timing and WebGPU compute-pass timing both batch GPU-resident operations. Ordinary CUDA launches expose submission gaps. WebGPU wall time also includes encoding and completion waiting. Tiny differences can be noise.</p><label>Workload size <select id="scale"><option value="large">Large: 4M elements / 512² matrices / 262K particles</option><option value="small">Small: 256K elements / 128² matrices / 65K particles</option><option value="all">Both sizes</option></select></label><div class="scroll"><table><thead><tr><th>Kernel</th><th>WebGPU GPU µs</th><th>CUDA graph GPU µs</th><th>WebGPU / CUDA</th><th>CUDA ordinary GPU µs</th><th>WebGPU wall µs</th></tr></thead><tbody>${cards}</tbody></table></div><p>Nine samples; 2,048 repeats per sample, 512 for matrices. Full reduction chains and histogram clearing are included. Particle compute excludes rendering. Inputs reset outside each sample; buffers are reused inside the sample. Float32 reference checks pass; integer histograms match exactly. This measures these kernels, not optimized CUDA libraries or cold transfers.</p><p><a href="performance-comparison.md">Full methodology and reproduction commands</a> · <a href="comparison-summary.json">Combined raw results</a> · <a href="../">Open live particle app</a></p></main><script>const s=document.querySelector('#scale');function filter(){document.querySelectorAll('tbody tr').forEach(r=>r.hidden=s.value!=='all'&&r.dataset.scale!==s.value)}s.onchange=filter;filter();</script></html>`);
console.log(md);
