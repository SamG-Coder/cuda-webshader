import test from 'node:test';import assert from 'node:assert/strict';import {compile} from '../src/compiler/compiler.js';
test('Uint 2D texture formats propagate through device helpers',()=>{const a=compile('__device__ unsigned sample(cudaTextureObject_t t,float x){return tex2D<unsigned int>(t,x,-2.5f);} __global__ void k(unsigned*out,cudaTextureObject_t t){out[0]=sample(t,4.5f);}');assert.equal(a.metadata.textures[0].format,'r32uint');assert.equal(a.metadata.textures[0].coordinates,'pixel-uint');assert.match(a.wgsl,/texture_2d<u32>/);assert.throws(()=>compile('__global__ void k(unsigned*out,cudaTextureObject_t t){out[0]=tex2D<unsigned int>(t,0,0)+(unsigned)tex2D<float>(t,0,0);}'));});
