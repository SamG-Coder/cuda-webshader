import test from 'node:test';import assert from 'node:assert/strict';import {readFileSync} from 'node:fs';import {compile} from '../src/compiler/compiler.js';import {executeCPU} from '../src/compiler/cpu-oracle.js';
test('Local brace arrays evaluate left to right and zero-fill nested omissions',()=>{const a=compile(readFileSync(new URL('local-array-init.cu',import.meta.url),'utf8'),{workgroupSize:[32]}),out=new Int32Array(320);executeCPU(a,{out},{},[1]);for(let i=0;i<32;i++)assert.deepEqual([...out.slice(i*10,i*10+10)],[i,i+1,i+2,0,i+3,i+3,0,0,9,32]);});
test('Array initialization rejects excess elements, narrowing and const mutation',()=>{for(const body of ['int a[1]={1,2};','int a[2]={1.5f};','unsigned a[1]={-1};','int a[2][2]={1,2};','const int a[1]={1};a[0]=2;'])assert.throws(()=>compile('__global__ void k(){'+body+'}'));});
