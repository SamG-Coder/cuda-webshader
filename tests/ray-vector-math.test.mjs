import test from 'node:test';import assert from 'node:assert/strict';import {readFileSync} from 'node:fs';import {compile} from '../src/compiler/compiler.js';import {executeCPU} from '../src/compiler/cpu-oracle.js';
test('Ray vector operations preserve normalization, dot products, conversions and compound updates',()=>{const c=compile(readFileSync(new URL('./ray-vector-math.cu',import.meta.url),'utf8'),{workgroupSize:[1]}),output=new Float32Array(16);executeCPU(c,{input:Float32Array.from([3,0,4,1]),output},{},[1]);const expected=[.6,0,.8,1,.5,0,.5,1,.6,.5,.8,2.4,1.1,1.5,3.3,1];expected.forEach((v,i)=>assert.ok(Math.abs(output[i]-v)<2e-6));});
test('Ray vector operations reject mixed dimensions, integer vectors and unsupported compound targets',()=>{for(const body of ['float x=dot(make_float3(1.0f),make_float4(1.0f));','float x=normalize(1.0f);','float3 x=fminf(make_float3(1.0f),1.0f);','uint4 x=normalize(make_uint4(1u));','float4 x=make_float4(1.0f);x%=2.0f;'])assert.throws(()=>compile('__global__ void k(){'+body+'}'),/float vectors|compound/);});
