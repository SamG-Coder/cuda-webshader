import test from 'node:test';import assert from 'node:assert/strict';import {readFileSync} from 'node:fs';import {compile} from '../src/compiler/compiler.js';import {executeCPU} from '../src/compiler/cpu-oracle.js';import {histogramMergeFixture} from '../showcases/nvidia/histogram-merge-fixtures.js';
for(const [index,bins]of [[31,64],[32,256]])for(const count of [0,17,513])test(`Original ${bins}-bin merge with ${count} partial histograms`,()=>{const source=readFileSync(`showcases/nvidia/kernels/${index}.cu`,'utf8'),c=compile(source,{workgroupSize:[256,1,1]}),f=histogramMergeFixture(bins,count,16);executeCPU(c,f.buffers,f.scalars,f.groups);assert.deepEqual(f.buffers.d_Histogram,f.expected);assert.equal(c.metadata.bindings.find(b=>b.name==='d_PartialHistograms').readOnly,true);});
