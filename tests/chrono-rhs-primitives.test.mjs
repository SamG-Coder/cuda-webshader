import test from 'node:test';import assert from 'node:assert/strict';import {readFileSync} from 'node:fs';import {compile} from '../src/compiler/compiler.js';import {executeCPU} from '../src/compiler/cpu-oracle.js';
test('Long ordered expressions, unary records and forwarded local arrays match native',()=>{const artifact=compile(readFileSync(new URL('chrono-rhs-primitives.cu',import.meta.url),'utf8'),{entry:'prerequisites',workgroupSize:[32]}),output=new Float32Array(128);executeCPU(artifact,{input:new Float32Array([1e8,1,-1e8,-2.75]),output},{},[1]);assert.deepEqual(Buffer.from(output.buffer),readFileSync(new URL('../reports/chrono-rhs-primitives-native.bin',import.meta.url)));});
