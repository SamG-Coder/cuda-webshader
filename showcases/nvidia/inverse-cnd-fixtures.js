// Independent normal-tail quadrature and bisection, not NVIDIA's Moro polynomial.
function tail(x){const steps=512,end=12,h=(end-x)/steps;let sum=Math.exp(-x*x/2)+Math.exp(-end*end/2);for(let i=1;i<steps;i++){const t=x+i*h;sum+=(i%2?4:2)*Math.exp(-t*t/2);}return h*sum/(3*Math.sqrt(2*Math.PI));}
export function inverseNormalReference(value){const upper=value>=0x80000000,low=upper?0xffffffff-value:value,p=(low+0.5)/0xffffffff;let a=0,b=10;for(let i=0;i<45;i++){const mid=(a+b)/2;if(tail(mid)>p)a=mid;else b=mid;}return (upper?1:-1)*(a+b)/2;}
export function inverseCndFixture(n=64,groups=1,guards=0){const special=[0,1,2,0x7fffffff,0x80000000,0xfffffffe,0xffffffff,343597383,343597384,3951369911,2147483646],d_Input=Uint32Array.from({length:Math.max(1,n)},(_,i)=>i<special.length?special[i]:Math.imul(i,2654435761)>>>0),d_Output=new Float32Array(Math.max(1,n)+guards).fill(-12345),expected=Array.from(d_Output);for(let i=0;i<n;i++)expected[i]=inverseNormalReference(d_Input[i]);return {buffers:{d_Input,d_Output},scalars:{pathN:n},groups:[groups,1,1],out:'d_Output',expected,absoluteTolerance:0.00002};}
